# If we import any file then it will firstly execute whole file.
# & If we don't want to execue whole file then we use main function.

# It is way to declare main function in file just like c++.

# Code written in "main" can't be executed if we import file in another fie. Can only be used within file

import main

print(main.add(5,8))